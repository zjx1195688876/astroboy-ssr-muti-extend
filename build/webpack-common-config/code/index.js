const appCode = require('./app');
const entryClientCode = require('./entry-client');
const entryServerCode = require('./entry-server');

module.exports = {
  appCode,
  entryClientCode,
  entryServerCode
};